%h3
  %i.editing-icon.editing-icon-eye{title: t('.details')}

  %span.title
    = @obj.name

  %i.editing-icon.editing-icon-trash.editing-right.delete-button{title: t('.delete'), data: {id: @obj.id}}

.inspector-content
  = yield
